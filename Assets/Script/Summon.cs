using UnityEngine;
using System.Collections;

public class Summon : MonoBehaviour {

	// Use this for initialization
	void Start () {
        CameraFade.StartAlphaFade(Color.white, true, 0.4f, 1f);
    }
	
	// Update is called once per frame
	void Update () {
    }
}
